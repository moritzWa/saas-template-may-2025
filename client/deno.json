{
  "tasks": {
    "dev": "deno run -A npm:next dev --webpack",
    "build": "deno run -A npm:next build",
    "start": "deno run -A npm:next start",
    "test": "deno run -A npm:playwright test",
    "test:ui": "deno run -A npm:playwright test --ui"
  }
}
